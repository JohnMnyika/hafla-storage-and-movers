import Head from 'next/head';

export default function Home() {
    return (
        <div style={{ backgroundColor: 'blue', height: '100vh', width: '100vw', margin: 0, padding: 0 }}>
            <Head>
                <title>Please pay the developers to fix this page</title>
                <meta name="description" content="A plain blue page." />
            </Head>
        </div>
    );
}